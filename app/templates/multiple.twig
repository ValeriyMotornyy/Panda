<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>Panda API Viewer</title>
        <link href='//fonts.googleapis.com/css?family=Lato:300' rel='stylesheet' type='text/css'>
        <link href="../bower_components/skeleton/css/skeleton.css" rel="stylesheet">
        <link href="../css/app.css" rel="stylesheet">
    </head>
    <body>
        <div class="container">
            <div class="row">
                <h3 class="left">Panda API result for: {{ title }}</h3>
            </div>
            <br>
            <div class="row">
                <h5 class="left">Description:
                    <span class="description">{{ desc }}</span>
                </h5>
            </div>
            <br>
            {% if count %}
                <div class="row">
                    <h5 class="left">Count:
                        <span class="description">{{ count }}</span>
                    </h5>
                </div>
                <br>
            {% endif %}
            <table class="u-full-width">
                <thead>
                    <tr>
                        <th>Key</th>
                        <th>Value</th>
                    </tr>
                </thead>
                <tbody>
                    {% for obj in object %}
                        {% for key,value in obj %}
                            <tr>
                                <td>{{ key }}</td>
                                <td>{{ value }}</td>
                            </tr>
                        {% endfor %}
                        <tr><td></td><td></td></tr>
                        <tr><td></td><td></td></tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </body>
</html>
